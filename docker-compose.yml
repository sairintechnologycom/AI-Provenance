version: '3.8'

services:
  merge-brief:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
